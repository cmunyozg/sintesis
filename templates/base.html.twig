<!DOCTYPE html>
<html class="h-100">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {% block title %}
        Síntesis
        {% endblock %}
    </title>
    <link rel="shortcut icon" type="image/ico" href="{{asset('img/favicon.ico')}}" />

    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('css/estilos.css') }}">
    {% block css %}{% endblock %}
    <script src="https://kit.fontawesome.com/89aa4d3e9c.js" crossorigin="anonymous"></script>
    <script src="{{ asset('js/jquery-3.4.1.min.js') }}"></script>
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
    <script src="{{ asset('js/headerFixed.js') }}"></script>
    <script src="{{ asset('js/avisoCookies.js') }}"></script>
</head>

<body class="container-fluid d-flex flex-column h-100">

    <!-- HEADER FIXED -->
    <div class="headerFixed row justify-content-around oculto">
        <!-- LOGO -->
        <div class="d-flex col-auto"><a href="{{path('principal')}}"><img
                    src="{{ asset('img/logo-negro.png') }}" height="70px" alt=""></a>
        </div>

        <!-- NAV -->
        <nav class="d-flex navFixed navbar navbar-expand">

            <ul class="navbar-nav mr-auto">

                <li class="nav-item dropdown mx-1">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        CATEGORÍAS
                    </a>
                    {{ render(controller('App\\Controller\\GeneralController::categoriasMenu')) }}
                </li>

                <li class="nav-item mx-1">
                    <a class="nav-link" href="{{path('evento_new')}}">PUBLICAR EVENTO <i class="fas fa-plus-circle"
                            aria-hidden="true"></i></a>
                </li>

                <li class="nav-item mx-1">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link" href="{{path('usuario_datos')}}">{{app.user.alias|upper}} <i class="fas fa-user"
                            aria-hidden="true"></i></a>
                    {% else %}
                    <a class="nav-link " href="{{path('app_login')}}">INICIAR
                        SESIÓN <i class="fas fa-user" aria-hidden="true"></i></a>
                    {% endif %}

                </li>


            </ul>


        </nav>
    </div>

    <!-- HEADER -->
    <header class="row justify-content-around">
        <!-- LOGO -->

        <div class="d-flex col-auto mx-3"><a href="{{path('principal')}}"><img
                    src="{{ asset('img/logo.png') }}" alt="" srcset=""></a></div>
        <!-- NAV -->
        <nav class="navHeader navbar navbar-dark navbar-expand-sm align-self-center justify-content-center p-1">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader"
                aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarHeader">
                <ul class="navbar-nav mr-auto">

                    <li class="nav-item dropdown mx-1 d-none d-sm-block">
                        <a class="navLinkHeader nav-link text-white dropdown-toggle" href="#" id="navbarDropdown"
                            role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            CATEGORÍAS
                        </a>
                        {{ render(controller('App\\Controller\\GeneralController::categoriasMenu')) }}
                    </li>

                    <li class="nav-item mx-1">
                        <a class="navLinkHeader nav-link text-white" href="{{path('evento_new')}}">PUBLICAR EVENTO <i
                                class="fas fa-plus-circle" aria-hidden="true"></i></a>
                    </li>
                    <li class="nav-item mx-1">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="navLinkHeader nav-link text-white btnSesion"
                            href="{{path('usuario_datos')}}">{{app.user.alias|upper}} <i class="fas fa-user"
                                aria-hidden="true"></i></a>
                        {% else %}
                        <a class="navLinkHeader nav-link text-white btnSesion" href="{{path('app_login')}}">INICIAR
                            SESIÓN <i class="fas fa-user" aria-hidden="true"></i></a>
                        {% endif %}

                    </li>


                </ul>

            </div>
        </nav>
    </header>
    {% block buscador %}{% endblock %}

    <!-- CONTENIDO -->
    <main class="row justify-content-center">
        <div class="col-12 titulo">{% block tituloSeccion %}{% endblock %}</div>


        <div class="col-12 col-sm-11 col-xl-10 contenedorContenido no-gutters">
            {% block contenido %}{% endblock %}
        </div>

    </main>
    <!-- COOKIES-->
    <div class="d-flex justify-content-center">
        <div id="divCookies" class="alert alert-warning col-sm-9 text-center oculto" role="alert">
            <p>
                En esta página utilizamos cookies para mejorar la navegación de nuestros
                usuarios y mejorar nuestros servicios. Si acepta o continúa navegando, consideramos que acepta su uso.

            </p>
            <strong onclick="aceptarCookies()" class="pointer">Aceptar cookies.</strong>
        </div>
    </div>
    <!-- FOOTER -->
    <footer class="row mt-auto">
        <div class="col-12">
            <h4 class="mt-3"><img src="{{ asset('img/logo-negro.png') }}" height="40px" alt="" srcset=""> © 2020
            </h4>
            <a href="#"><i class="fab fa-facebook fa-3x" style="color: #3b5998"></i></a>
            <a href="#"><i class="fab fa-instagram fa-3x ml-2" style="color: #3f3f3f"></i></a>
            <a href="#"><i class="fab fa-twitter fa-3x ml-2" style="color: #00acee"></i></a>
            <p class="my-2">
                <a href="#">Ver términos y condiciones</a>
            </p>
        </div>
    </footer>




    {% block javascripts %}{% endblock %}
</body>

</html>